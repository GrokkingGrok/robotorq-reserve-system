[package]
name = "triples-deprecated"
version = "0.0.0"
edition = "2021"
publish = false

[package.metadata]
deprecated = true
reason = "Use common crate: common::triples"

[lib]
path = "src/lib.rs"

[dependencies]
# No dependencies; crate is deprecated stub.