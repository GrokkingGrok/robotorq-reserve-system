pub async fn start_http_server(_host: String, _port: u16) {
    tracing::info!("http server stub started");
    // TODO: Implement a lightweight HTTP server exposing /health and /metrics.
    // For the MVP scaffold we keep a stub that does not bind sockets.
}
